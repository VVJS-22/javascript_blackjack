import styled from "styled-components/macro"

export const Wrapper = styled.section`
    min-height: 50px;
    color: #fff;
    position: relative;
    h1 {
        text-align: center;
        font-size: 2rem;
        padding: 1rem 1.2rem;
    }
    button {
        position: absolute;
        top: 1.25rem;
        right: 1.25rem;
    }
`
